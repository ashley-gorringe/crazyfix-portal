{% extends "_layout_account.twig" %}

{% set menuSlug = "account" %}
{% set pageTitle = "Account" %}

{% set subMenuSlug = "general" %}

{% block main %}
<section>
    <div class="container --medium">
        <h1>General</h1>
        <p class="lead">Manage your CrazyFix Automotive Portal Account.</p>
    </div>
</section>
<section>
    <form class="process-form page-form">
        <input type="hidden" name="action" value="account-general" />
        <dialog class="form-actions">
            <div class="form-actions-container">
                <span>You have unsaved changes.</span>
                <button type="button" class="button --quiet page-form-cancel-button">Cancel</button>
                <button type="submit" class="button --primary">Save Changes<i data-feather="loader"></i></button>
            </div>
        </dialog>
        <div class="form-section">
            <h2>Your Details</h2>
            <p>Update your personal information, such as your name, email address, and profile picture.</p>
            <div class="form-group">
                <label>Picture</label>
                <a href="https://evatar.io/set/{{customer.email_address}}" target="_blank" class="profile-picture-field">
                    <div class="picture" style="background-image: url(https://www.evatar.io/{{customer.avatar_hash}});"></div>
                    <span>Change your profile picture on Evatar</span>
                </a>
                <div class="form-help">
                    Let's put a face to the name! Upload a photo or avatar that represents you. It will be used as your profile picture in comments and other places around ServerBook. We don't store your picture here, we use an external service called Evatar, so you'll be taken there to upload or edit your picture.
                </div>
            </div>
            <div class="form-group">
                <label for="name">First Name</label>
                <input type="text" name="first_name" value="{{ customer.first_name }}" class="form-control --auto-width" />
                <div class="form-error"></div>
            </div>
            <div class="form-group">
                <label for="name">Last Name</label>
                <input type="text" name="last_name" value="{{ customer.last_name }}" class="form-control --auto-width" />
                <div class="form-error"></div>
            </div>
            <div class="form-group">
                <label for="email">Email Address</label>
                <input type="text" name="email" value="{{ customer.email_address }}" class="form-control --auto-width" />
                <div class="form-error"></div>
                <div class="form-help">
                    This is the email address that you'll use to log in. We'll also use it to send you important notifications.
                </div>
            </div>
        </div>
    </form>
</section>
{% endblock %}
